// now we know that we can use variables.

var myName = "John";
let myAge = 30;
const myCountry = "USA";
// Variables can be reassigned if they are declared with var or let, but not with const.

console.table([myName, myAge, myCountry]);

// * peffer not use var, use let or const instead
// * const is used for variables that should not change, like configuration values or constants.

let naeme = "John";

console.log(naeme);
